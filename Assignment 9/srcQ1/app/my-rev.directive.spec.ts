import { MyRevDirective } from './my-rev.directive';

describe('MyRevDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRevDirective();
    expect(directive).toBeTruthy();
  });
});
